import React, { useEffect, useState } from "react";
import Router from 'next/router';

const CustomAnswer = () => {

  useEffect(() => {
    
  });

  return (
    <React.Fragment>
      
    </React.Fragment>
  );
};

export default CustomAnswer;
